# fullstack_model
Using AWS Sagemaker, and AWS Lambda to take image input, for trained machine learning image classfication model, from locally hosted webpage and return model classification to be displayedo on webpage.
